export enum ClearType {
    NONE,
    ALL,
    SPECIFIC,
}

export enum ActionModes {
    VIS_AXES,
    SELECT_POINTS
}

// TODO: Move the export block into the radio button section itself
export enum FileAnnotationActions {
    NONE, 
    CLEAR_ALL,
    CLEAR_SPECIFIC,
    EXPORT
}
